version: '3'

services:
  app:
    image: qqpy3ko/devops-for-programmers-project-74
    build: 
      context: ./
      dockerfile: Dockerfile.production
    environment:
      - NODE_ENV=test
    command: npm test